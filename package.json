{
  "dependencies": {
    "concurrently": "^9.1.2",
    "express": "^5.1.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:frontend": "cd client && npm run dev",
    "dev:backend": "ts-node-dev server.ts",
    "dev:tscCompiler": "tsc --watch",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:tscCompiler\""
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "ts-node-dev": "^2.0.0"
  }
}
